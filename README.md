# Example CRM UI

This project was built with [Angular](https://angular.dev/), [Angular Material](https://material.angular.dev/), and JavaScript. From the project directory, run `npm install` to download the dependencies and `ng serve` (or `npm start`) to start the application. The page will exist locally at http://localhost:4200/.
