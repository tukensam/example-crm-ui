# Example CRM UI

This project was built with [Angular](https://angular.dev/), [Angular Material](https://material.angular.dev/), and JavaScript. From the project directory, you can start the application by running `ng serve` (or `npm start`) in your terminal. The page will exist locally at http://localhost:4200/.